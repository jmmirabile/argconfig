parser:
  prog: test-check
  description: test-check - CLI application
  epilog: Use 'test-check --help' for more information
parent_arguments:
- name: --log-level
  short: -l
  type: str
  choices: '@logging_levels'
  default: INFO
  help: Set logging level
arguments:
- name: --api-key
  help: API key for authentication
  type: str
  env_var: API_KEY
subcommands: null
